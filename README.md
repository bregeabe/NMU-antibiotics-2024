## Initial README
blah blah blah blah blah blah


for new branch: ``` git checkout -b new-branch-name ```
to navigate to existing branch: ``` git checkout branch-name ```
to save: ``` git stash ```
to pull saved: ``` git stash pop (number if you know it) ```
